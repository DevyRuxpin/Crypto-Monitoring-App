# Database Configuration (Render will provide these automatically in production)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/crypto_monitor

# Security (Make sure to change this in production)
JWT_SECRET=your_super_secret_key_change_this_in_production
JWT_EXPIRATION_HOURS=24

# Server Configuration
PORT=8080  # Render will provide this automatically in production
ENVIRONMENT=development

# Redis URL 
REDIS_URL=redis://localhost:6379
